<Shell xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
       xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
       xmlns:local="clr-namespace:WordleGame"
       x:Class="WordleGame.AppShell">
    <!-- Define the navigation structure -->
    <ShellContent Title="Main Page" ContentTemplate="{DataTemplate local:MainPage}" />
    <ShellContent Title="Game Page" ContentTemplate="{DataTemplate local:GamePage}" />
    <ShellContent Title="History Page" ContentTemplate="{DataTemplate local:HistoryPage}" />
</Shell>
